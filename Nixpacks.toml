[phases.setup]
nixPkgs = ['...', 'php83', 'nodejs'] # Sesuaikan versi PHP Anda (misal php82 atau php83)

[phases.build]
cmds = ['composer install --no-dev --optimize-autoloader', 'npm install && npm run build']

[start]
cmd = 'php artisan serve --host 0.0.0.0 --port $PORT'
